# Copy to .env (gitignored) for `just package`. Signing + notarization identities.
APP_IDENTITY="Developer ID Application: NAME (TEAMID)"
PKG_IDENTITY="Developer ID Installer: NAME (TEAMID)"
NOTARY_PROFILE="AC_NOTARY"
